# VerdeGrid — Project Context for Claude Code

## What this repo is

VerdeGrid is a datacenter sustainability platform: 5 modules (PowerPrune, GridSync, CoolSense, NetPulse, ThermOS) covering server consolidation, carbon-aware job scheduling, water/cooling monitoring, network optimization, and thermal prediction. Two parts:

- `datacenter-os/backend/` — Python/FastAPI. All real logic lives here.
- `datacenter-os/frontend/` — React 18 + Vite dashboard. Currently runs on local mock data (`src/data/mock/*.js`), fully disconnected from the backend. **Do not touch the frontend until told to** — see "Current phase" below.
- `core/`, `plugins/`, `index.js` at repo root — a small, unrelated Node plugin-loader experiment. Not part of VerdeGrid proper; leave alone unless explicitly asked.

## Required reading before starting any phase

Read these, in this order, before writing code:

1. `docs/BUILD_PLAN.md` — the phased build plan, translated to our actual stack. **This is the plan of record.**
2. `docs/VerdeGrid-Implementation-Methodology.md` — exact algorithms, schemas, pseudocode, and acceptance tests per item.
3. `docs/VerdeGrid-Consolidated-Change-List.md` — the full 64-item change list with rationale, if you need the "why" behind an item.
4. The relevant per-module `docs/*-Deep-Research-Architecture-Optimization.docx` — only if you need more background than the methodology doc already distilled. Usually you won't need these.

## Key decisions already made (do not re-litigate these)

- **Telemetry source**: no real hypervisor/BMC/SNMP/Electricity-Maps hardware access exists. Build a stateful synthetic telemetry simulator (`shared/telemetry_sim.py`, Phase 0) with realistic time series — real variance, trends, injectable anomalies — so the actual algorithms (MAD thresholds, dwell timers, elephant-flow detection, etc.) run on genuinely plausible data. This is real, reusable engine code, not a stopgap to throw away later. Write real client adapters behind an interface so a future real integration can swap in without touching algorithm code.
- **Sequencing**: build the entire backend first (all phases through Phase 8). Frontend wiring is Phase 9, done in one dedicated pass at the end — don't wire any dashboard module to the real backend before then, even if it would be easy to.
- **Stack translation**: the methodology doc's TypeScript interfaces translate directly to Pydantic `BaseModel`s with the same field names/shapes. Its "event bus" becomes an in-process pub/sub (`shared/eventbus.py`) — no need for a real message broker. No WebSockets — REST polling at 3–5s (the frontend's existing cadence) satisfies the methodology's 30–60s telemetry cadence with room to spare.
- **`api/routes.py` stays a single file** (not split per-module) — but its endpoint bodies should become thin wrappers delegating to the real module packages (`powerprune/`, `gridsync/`, etc.), not contain logic inline.
- **Fail-safe-open default, everywhere**: every classification defaults to the safest state (`protected` not `deferrable`, `normal` not `idle-candidate`) until explicitly overridden by data or an operator. Every MUST HAVE item needs an automatic fallback to current (pre-change) behavior if its new dependency is unavailable.

## Current phase

<!-- Update this line as we progress. Example: -->
<!-- Currently on: Phase 0 — shared infrastructure (contracts, event bus, classification, telemetry simulator) -->
Phases 0-7 complete (all 22 MUST HAVE + all 16 SHOULD HAVE items). Phase 0: `shared/contracts.py`, `shared/eventbus.py`, `shared/classification.py`, `shared/telemetry_sim.py`. Phase 1: `powerprune/threshold.py` (#1 MAD threshold), `powerprune/consolidation.py` (#2 migration-cost check, #3 classification hard filter, #4 redundancy-aware placement), `powerprune/power.py` (#5 dwell/wake state machine), `powerprune/telemetry.py` (simulator-backed adapter). Phase 2: `thermos/sensors.py` (#21 airflow/pressure sensing — `PressureReading` defined here for CoolSense's Phase 4 MUST HAVE #10 to reuse), `thermos/model.py` (#20 physics-lite RC core + ML residual corrector, #18 MC-Dropout-style uncertainty bands, #19 PowerPrune telemetry join). Phase 3: `gridsync/jobs.py` (#6, #7), `gridsync/grid.py` (#8), `gridsync/scheduler.py` (core capacity-curve + #9, emits `gridsync.prewake.requested`). Phase 4: `coolsense/sensors.py` (#10, #11), `coolsense/baseline.py` + `coolsense/anomaly.py` (#12, #13). Phase 5: `netpulse/flow.py` (#14), `netpulse/congestion.py` (#15), `netpulse/routing.py` (#16, #17 — `resolve_latency_sensitivity` reused `shared/classification.py` directly, IP→VM auto-lookup deferred to SHOULD HAVE #21). Phase 6: `thermos/control.py` (#22 supervised closed-loop control, trust ladder never auto-enables). Phase 7 (all 16 SHOULD HAVE, any order): `powerprune/resilience.py` (#6 failure handling), `powerprune/consolidation.py` extended (#7 thermal-headroom cross-wire), `powerprune/telemetry.py` extended (#8 K8s adapter, proven adapter-agnostic); `gridsync/grid.py` extended (#10 `ForecastCache`, #11 `HysteresisState`), `gridsync/scheduler.py` extended (#12 price-weighted ranking); `coolsense/anomaly.py` extended (#14 point-sensor trip-wire), `coolsense/sensor_health.py` (#15 flatline/bounds/missing-sample fault detection), `coolsense/cooling.py` (#16 cooling-performance cross-wire); `netpulse/congestion.py` extended (#18 queue-depth/packet-loss OR conditions), `netpulse/topology.py` (#19 LLDP discovery), `netpulse/telemetry.py` extended (#20 SNMP fallback), `netpulse/routing.py` extended (#21 IP→VM auto-lookup, completing Phase 5's deferred piece); `thermos/spatial.py` (#23 — NOTE: implements grid-shaped spatial residual smoothing + IDW sensor-vs-interpolated flagging as an honest, buildable stand-in for real trained ConvLSTM2D, since no DL framework is in this project's dependencies — flagged explicitly, not silently substituted), `thermos/zoning.py` (#24 correlation-based zoning + adaptive setpoints feeding the ActionRecommendation queue), `thermos/maintenance.py` (#25 predictive maintenance). All tested, full suite passing (243 tests). Phase 8 checked and closed with no changes made: both MODIFY target phrases ("horizontal integration", "a prediction model with a dashboard") were searched for across the whole repo and don't exist as live copy anywhere yet — no root README, no pitch-deck file, no backend code comments contain them. The only plausible live-copy location is frontend module descriptions, which are off-limits until Phase 9. Decision (confirmed with the user): apply this positioning copy during Phase 9 when ThermOS's frontend description actually gets written, rather than pre-creating documentation now. Phase 8b (cross-module orchestration, inserted after a verification pass found all 7 dependency-map dependencies were type-compatible but never actually called): `shared/orchestrator.py` now holds one real function per dependency, each taking real adapter/telemetry instances and calling the downstream module's real function with real data -- `build_rack_feature_vector` (PowerPrune→ThermOS), `compute_thermal_headroom`/`filter_consolidation_targets_by_real_headroom` (ThermOS→PowerPrune), `compute_capacity_forecast`/`schedule_job_with_real_capacity` (PowerPrune→GridSync), `PrewakeSubscriber` (GridSync→PowerPrune, a real event-bus subscriber that calls `DwellStateMachine.request_wake()`), `bucket_rack_load` (PowerPrune→CoolSense), `compute_rack_cooling_performance` (ThermOS→CoolSense), `apply_operator_classification`/`tag_flow_with_real_classification` (PowerPrune→NetPulse -- `apply_operator_classification` is the one production path that calls `classification_store.set_tag()`). Also fixed `powerprune/consolidation.py`'s `filter_consolidation_candidates` to default to the shared `classification_store` singleton (was previously required-argument-only, unlike `netpulse/routing.py`'s equivalent). Added the missing GridSync integration test proving deadline force-run despite a dirty grid (`tests/test_gridsync_deadline_vs_dirty_grid.py`) -- neither prior test actually covered this. Phase 8b fix round (review found the dirty-grid test was two disconnected halves, and that `force_release_overdue`'s return value was never consumed by anything -- the same "publish to nobody" gap as prewake): added `gridsync/jobs.py`'s `DeadlineQueue.force_run_overdue()`, the real production path that force-releases overdue jobs and publishes `gridsync.job.scheduled` (a topic defined since Phase 0, never published/subscribed until now); added `shared/orchestrator.py`'s `JobExecutionTracker`, the real subscriber recording which jobs actually ran. Rewrote the dirty-grid test as one continuous scenario: same job/pool/windows attempted repeatedly against the real scheduler and confirmed blocked throughout, then force-run at the exact deadline with a real subscriber observing the effect. Full suite passing (267 tests). Phase 8c (periodic driver, since force_run_overdue and PrewakeSubscriber only did the right thing when something called them at the right moment -- nothing did on its own in a running system): investigation of `powerprune/power.py` found `DwellStateMachine` has zero internal clock -- every transition is purely reactive to explicit calls, and `wake_confirmed()` was called nowhere in production code, so a host moved to WAKING by `PrewakeSubscriber` stayed there forever. Built `shared/scheduler_driver.py`: `SchedulerRegistry` (central registry of live `DeadlineQueue`/`DwellStateMachine` instances) and a pure `tick(now)` that calls `force_run_overdue` on every registered queue and confirms any `WAKING` machine whose recorded wake-start is old enough. Caught and fixed a real bug during this build: `PrewakeSubscriber` initially recorded wake-start with real `datetime.now()`, which conflicted with `tick()`'s simulated `now` in tests -- fixed by making `tick()` the sole timekeeper (it records wake-start itself on the first tick that observes a host WAKING); `PrewakeSubscriber` reverted to its exact Phase 8b shape. Wired a real interval loop into `main.py`'s FastAPI lifespan (`SCHEDULER_TICK_INTERVAL_SECONDS`, default 5s) that calls `tick(now=real utcnow())` and shuts down cleanly. Full suite passing (276 tests). Phase 9 (frontend integration): backend side is `api/state.py` (a fixed 5-rack x 4-host x 8-link simulated topology, module-level singletons, every DwellStateMachine/DeadlineQueue registered into `shared.scheduler_driver.scheduler_registry` -- closing the Phase 8c gap) and `api/routes.py` rewritten so every endpoint delegates to the real module packages instead of canned/random data, matching the frontend's existing mock field-name shapes where practical. New real endpoints: PowerPrune classification GET/PATCH (MUST HAVE #3), CoolSense maintenance-mode declare/read (SHOULD HAVE #13/#15), ThermOS action-queue GET/approve/reject (MUST HAVE #22), NetPulse inject-spike (demo control using the real telemetry sim). Small real additions along the way: `CarbonForecastSimulator.sample_current()`/`.history()` (live polling, it only had 48h-batch `forecast_48h` before) and `DeadlineQueue.remove()` (manual run-now). Full backend suite: 286/286 passing, all verified via direct HTTP calls (not just pytest). Frontend: `src/services/api.js` re-introduced (was "removed earlier as dead code") plus one thin wrapper module per backend module; `src/hooks/useLiveResource.js` replaces each module's mock `getSnapshot()/subscribe()` pattern; `vite.config.js`'s dev proxy re-added. All 5 modules (`index.jsx`, not the dead `Dashboard.jsx` files) + `overview` + `App.jsx` rewired to real data; GridSync needed the most net-new work since it had zero external data dependency before. Three new UI pieces per BUILD_PLAN.md's Phase 9 scope: `powerprune/ClassificationOverride.jsx`, `coolsense/MaintenanceModeToggle.jsx`, `thermos/ActionApprovalQueue.jsx`. The deferred ThermOS copy change ("a prediction model with a dashboard" -> "a Physics + Data + Control system") did not apply -- grepped the frontend and that phrase was never actually present, so there was nothing to change. Phase 9 verification pass (Node.js was available this session, unlike the caveat above): `npm install && npm run build` and a full headless-browser pass (Playwright/Chromium) against the live dev server + real backend confirmed all 6 pages (Overview, PowerPrune, CoolSense, GridSync, ThermOS, NetPulse) render live data with zero console errors, and every write-path control actually round-trips to the backend (GridSync Run Now, ThermOS Approve, PowerPrune classification override -> real `200 PATCH`, CoolSense Declare Window -> real `200 POST`, NetPulse More Traffic) plus the full onboarding-wizard-to-dashboard flow. Found and fixed one real bug during that pass: `gridsync/index.jsx`'s `MetricCard` `title` prop was passed an arrow function instead of JSX (`MetricCard` renders `{title}` directly as a child, doesn't call it), silently dropping the LIVE/SPIKE badge and logging a React error every render -- fixed by passing the JSX directly. Also added a working frontend ESLint setup (`eslint.config.js` -- `npm run lint` previously crashed immediately, no config existed) and fixed the ~20 pre-existing findings it surfaced (dead code in superseded `Dashboard.jsx` files, `AIAgent`'s `placeholder` prop never wired to its actual `<input>`, a few others -- all itemized in the merged PR description). Landed via srianirudhvarma-del/verdegridf#1, merged to `main`.

**Known, deliberately-not-fixed gaps surfaced during that pass** (worth knowing before a demo/pitch): (1) `SavingsMeter.jsx`'s "Energy Offset Savings" tile on the PowerPrune dashboard is purely decorative -- a `setInterval` client-side counter that ticks up forever with no real time or backend basis (`wattsIdle` hardcoded to 200, ignoring the backend's real per-server values), resets to 0 on page refresh; the component's own comment admits it ("for visual we animate"). (2) The onboarding wizard's "AI Deployment Summary" panel calls OpenRouter/DeepSeek directly from the browser (`AIAgent.jsx`'s `callDeepseek`) using `VITE_DEEPSEEK_API_KEY`, which is never set anywhere in this repo -- fails with a visible but non-fatal "Error: Failed to fetch" / "missing authentication header" until that env var is set on whatever host serves the frontend; also, being a `VITE_`-prefixed var, it ships in the public JS bundle, so this should move server-side (through the FastAPI backend) before any real deployment. (3) The onboarding wizard's deployment-plan ROI/payback numbers (`logic/configurator.js`) are a static hardcoded-percentage-of-electricity-bill heuristic (8%/5%/6%/idle-server-wattage assumptions), not backend-derived -- real hardware catalog and pricing, illustrative savings math. (4) `npm audit` flags 2 vulnerabilities (1 moderate, 1 high), both esbuild dev-server-only CORS issues transitively via Vite 5; only fix is a Vite 8 major-version bump, not done.

**Deployment**: not deployed anywhere live. Backend cannot run on Vercel (or any serverless host) as-is -- `api/state.py` holds long-lived in-memory singletons (`DwellStateMachine`/`DeadlineQueue` instances, the simulated topology) and `main.py`'s FastAPI lifespan runs a continuous background loop (`scheduler_driver.tick()`, every 5s) that serverless invocation models can't support. If/when deploying: frontend to Vercel (or similar) with `VITE_API_BASE_URL` pointed at the backend's public URL; backend needs a host that keeps one process alive continuously (Render/Railway/Fly.io/a VPS).

**Git history note**: this repo's git history was intentionally squashed and force-pushed on 2026-09-19 (full pre-squash backup taken first) -- real content, byte-for-byte identical to what was on `main` before, just reorganized into 5 clean commits (setup/config, backend, frontend, docs, the unrelated `core`/`plugins` Node experiment) instead of the original many small incremental commits. Not a fabricated timeline; every commit's content is exactly what it claims to be.

Next up: nothing planned -- all 22 MUST HAVE + 16 SHOULD HAVE backend items, orchestration/driver layers, and frontend wiring are done and browser-verified. If picking this up again: the four "known gaps" above are the natural next things to address, in roughly that priority order (SavingsMeter is the most visible/embarrassing in a live demo; the AI advisor key is the quickest real fix; the ROI math and npm audit are lower urgency).

**Real hardware integration (post-Phase-9, not part of the original 0-9 plan)**: the user wants to prove the whole architecture actually works against real data, not just the synthetic simulator -- two physical Windows laptops (HP OMEN Transcend 16, HP Victus 15) as real PowerPrune/ThermOS nodes, plus a third machine hosting the backend, all on the same LAN. This is exactly the "real client adapter" swap the Phase 0 telemetry decision asked for, and it's additive: the existing 5x4 simulated topology in `api/state.py` is untouched, real hosts live alongside it.

Backend (`datacenter-os/backend/`): `shared/real_agent.py` -- `RealAgentSample`/`RealAgentRegistry` (ingest + rolling history + `is_stale()` fail-safe-open staleness gate) and `predict_hotspot()` (a genuine linear-trend extrapolation of CPU package temp, `horizon_seconds` ahead, discounted from "critical" to "watch" when clock speed isn't near max -- guards against calling sensor noise on an idle chip a real hotspot; an already-over-limit *current* temp is always "critical" regardless of clock, since that part doesn't need the noise guard). `api/real_nodes.py` -- `RealCommandQueue` (one pending command per host; agent polls, pops, acks) and `RealNodeManager.tick()`, which reuses `powerprune/threshold.py::classify_host` and `powerprune/power.py::DwellStateMachine` completely unchanged against real telemetry instead of the simulator, edge-triggers exactly one `sleep_prompt` command per idle episode (not one per tick), and feeds hotspot predictions into the *existing* `thermos/control.py::ActionRecommendationQueue` (MUST HAVE #22's supervised approval queue already had the `adjust_fan_speed` action type; this is the first thing that actually produces and executes one for a real host) -- escalating to real fan control still requires operator approval in the dashboard, exactly like every other ThermOS action; only the de-escalation (`fan_max_off` once temp recovers) is automatic, matching the fail-safe-open shape used everywhere else in this codebase. `api/routes.py` adds `/api/real/telemetry` (ingest), `/api/real/commands/{host}` (agent poll) and its `/ack`, and `/api/real/hosts` (visibility only, not wired into the React dashboard -- out of scope for this pass); `approve_action` now enqueues a real `fan_max_on` command when the approved recommendation's `rackId` is a known real host, a no-op for simulated racks. `main.py`'s existing scheduler-driver loop also calls `real_node_manager.tick()` every cycle. New tests: `tests/test_shared_real_agent.py`, `tests/test_api_real_nodes.py`, `tests/test_api_real_routes.py`. Full suite passing (310 tests).

Real-hardware agent (`datacenter-os/real-agent/`, new top-level dir, Windows-only, not part of the pytest suite -- can't be exercised from this sandbox, no physical hardware here): `agent.py` reads real `psutil` CPU/mem/disk/network + `ctypes`-only Win32 idle detection (`GetLastInputInfo`) + CPU package temp via LibreHardwareMonitor's WMI provider (optional; `cpuTempC` is `null`, not fabricated, if LHM isn't running), POSTs to `/api/real/telemetry`, polls `/api/real/commands/{host}`, and acts: a `sleep_prompt` always shows a real Win32 `MessageBoxW` Yes/No dialog and only calls `SetSuspendState` on Yes -- no auto-sleep path exists; `fan_max_on`/`fan_max_off` shell out to an operator-supplied external command from `config.json` (`dryRun: true` by default). Deliberately does **not** hard-code HP's BIOS/EC fan-control command bytes itself: the open-source OmenMon project has reverse-engineered that interface for OMEN/Victus laptops, but this session's network egress to its CLI docs site (`omenmon.github.io`) was blocked, so its exact current flag syntax couldn't be verified against real hardware from here -- flagged explicitly in `real-agent/README.md` rather than guessing wrong BIOS-level commands, same "honest substitution, not silent fake" pattern as `thermos/spatial.py`'s Phase 7 ConvLSTM stand-in. The user needs to fill in `fanActuatorCommands` in `config.json` themselves after confirming the right command on their own machine (or leave it empty and demo everything up to, but not including, physical fan actuation).

Not done / next steps if picked up again: (1) actually run `agent.py` on the two real laptops and the backend on the third machine to verify the end-to-end LAN round-trip (agent → backend → dashboard approval → command → agent) -- untested outside of unit/route-level pytest against in-process singletons, since no physical hardware is reachable from this session; (2) confirm and fill in real `fanActuatorCommands` against the actual OMEN Transcend 16 / Victus 15 BIOS interface; (3) optionally, a small frontend panel surfacing `/api/real/hosts` (not built -- out of scope for this pass, frontend otherwise untouched).

## Working rhythm — follow this strictly

1. Implement **one phase at a time**, not the whole plan in one shot. Within a phase, one numbered MUST HAVE item at a time is even better if the phase is large.
2. After implementing an item: **write and run a test proving the specific acceptance criterion** from the methodology doc / Section 10 checklist — not just "it runs," but the actual documented behavior (e.g. "adaptive threshold widens with variance," "untagged VM never in candidate list").
3. Run the existing test suite (`npm test` at repo root for the JS plugin tests, `python3 -m pytest` in `datacenter-os/backend/` for the Python suite) to confirm nothing already-working broke.
4. Commit after each item or phase passes its tests. Small commits, not one giant diff.
5. Update the "Current phase" line above before ending a session, so the next session picks up correctly.
6. If a phase's acceptance test can't pass yet because an earlier phase's dependency isn't built — stop and say so, don't fake it with a stub that silently returns fixed data.

## Do not build

The methodology's Section 5 / change-list's Table 5 lists 13 explicitly out-of-scope items (fleet-scale custom scheduler, full SDN fabric replacement, CFD simulation, autonomous non-supervised control anywhere, etc.). Don't implement these even if a later phase seems to gesture toward them — check the anti-scope table first if something feels like it's drifting that direction.
